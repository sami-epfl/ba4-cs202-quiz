Question labels: 
- MCQ = multiple choice questions





(MCQ) What is the correct way to declare an integer variable in C?
- int x; (CORRECT ANSWER)
- integer x;
- var int x;
- x int;

(MCQ) What is the purpose of the main function in C?
- It is the entry point of the program (CORRECT ANSWER)
- It is an optional function
- It is only used for printing text
- It compiles the program

(MCQ) What is the result of 5 / 2 in C (integer division)?
- 2 (CORRECT ANSWER)
- 2.5
- 3
- 0

(MCQ) What is the correct format specifier for printing an integer in C?
- %d (CORRECT ANSWER)
- %f
- %c
- %s

(MCQ) Which keyword is used to create a loop in C?
- while (CORRECT ANSWER)
- loop
- repeat
- iterate

(MCQ) What does malloc do in C?
- Allocates dynamic memory (CORRECT ANSWER)
- Frees memory
- Declares a variable
- Compiles the program

(MCQ) What is the correct operator to compare two values in C?
- == (CORRECT ANSWER)
- =
- !=!
- <>

(MCQ) What does the return statement do in a function?
- It returns a value and terminates the function (CORRECT ANSWER)
- It prints a value
- It creates a variable
- It repeats the function

(MCQ) What is the typical size of an int in C on most modern systems?
- 4 bytes (CORRECT ANSWER)
- 2 bytes
- 8 bytes
- 1 byte

(MCQ) Which header file is required to use printf?
- #include <stdio.h> (CORRECT ANSWER)
- #include <stdlib.h>
- #include <string.h>
- #include <math.h>


(MCQ) What does a pointer store in C?
- A memory address (CORRECT ANSWER)
- A data type name
- A compiled instruction
- A function return value

(MCQ) Which operator is used to get the address of a variable?
- & (CORRECT ANSWER)
- *
- ->
- %

(MCQ) Which operator is used to dereference a pointer?
- * (CORRECT ANSWER)
- &
- ->
- $

(MCQ) What is the correct declaration of a pointer to int?
- int *p; (CORRECT ANSWER)
- int p*;
- pointer int p;
- *int p;

(MCQ) What does NULL represent in pointer context?
- A pointer pointing to nothing (CORRECT ANSWER)
- A pointer to zero
- A memory address 0x1
- An uninitialized variable

(MCQ) What happens in pointer arithmetic (e.g., p++)?
- It moves to the next memory block of the pointed type (CORRECT ANSWER)
- It adds 1 byte only
- It multiplies the value by 2
- It resets the pointer

(MCQ) What is the relationship between arrays and pointers in C?
- Array name acts as a pointer to the first element (CORRECT ANSWER)
- Arrays are unrelated to pointers
- Arrays store pointer types only
- Pointers cannot access arrays

(MCQ) What does malloc return in C?
- A void pointer to allocated memory (CORRECT ANSWER)
- An integer size
- A null-terminated string
- A compiled address label

(MCQ) What is a double pointer (int **p)?
- A pointer to another pointer (CORRECT ANSWER)
- A pointer to an integer array only
- A function pointer
- A null pointer

(MCQ) What does const int *p mean?
- The value pointed to cannot be modified through p (CORRECT ANSWER)
- The pointer itself cannot change address
- p is a constant integer
- p is a null pointer







(MCQ) What is the result of dereferencing a NULL pointer in C?
- Undefined behavior (CORRECT ANSWER)
- It returns 0
- It returns NULL safely
- It compiles but does nothing

(MCQ) What is a function pointer in C used for?
- Storing the address of a function to call it indirectly (CORRECT ANSWER)
- Allocating dynamic memory for functions
- Returning multiple values from a function
- Defining recursive functions only

(MCQ) What does the expression *(p + i) represent?
- The value at index i from pointer p (CORRECT ANSWER)
- The address of p shifted by i bytes only
- The function at position i
- A compilation error always

(MCQ) What happens if you call free() twice on the same pointer?
- Undefined behavior (CORRECT ANSWER)
- It safely ignores the second call
- It resets the pointer to NULL automatically
- It reallocates memory

(MCQ) What is pointer aliasing?
- Multiple pointers referencing the same memory location (CORRECT ANSWER)
- A pointer pointing to another pointer only
- A pointer with invalid type
- A pointer initialized to zero

(MCQ) What does realloc() do in C?
- Resizes previously allocated memory block (CORRECT ANSWER)
- Frees all allocated memory
- Creates a new variable on stack
- Converts pointer to integer

(MCQ) What is the risk of returning the address of a local variable?
- The pointer becomes dangling (CORRECT ANSWER)
- The variable becomes global
- Memory is automatically copied to heap
- The compiler fixes it automatically

(MCQ) What does const int *p guarantee?
- The value pointed to cannot be modified through p (CORRECT ANSWER)
- The pointer address cannot change
- Both pointer and value are immutable
- The pointer is stored in ROM

(MCQ) What is strict aliasing rule in C related to?
- Accessing the same memory through incompatible pointer types (CORRECT ANSWER)
- Faster loops in arrays
- Function overloading
- Memory allocation speed

(MCQ) What is the alignment requirement in pointer usage mainly about?
- Memory addresses must match type alignment constraints (CORRECT ANSWER)
- Pointers must always be 8 bytes
- All pointers must be initialized to NULL
- Alignment only affects arrays, not pointers





